# Optional: enables the AI fallback that reads product pages when the
# structured-data parsers can't find dimensions. Without it, add-by-link still
# works — users just type the dimensions themselves.
ANTHROPIC_API_KEY=
